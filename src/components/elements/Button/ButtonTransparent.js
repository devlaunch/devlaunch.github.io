import styled from 'styled-components';
import { ButtonTransparent } from 'rebass';

const StyledButtonTransparent = styled(ButtonTransparent)`
`;

StyledButtonTransparent.defaultProps = {
  color: 'gray.7'
}

export default StyledButtonTransparent;