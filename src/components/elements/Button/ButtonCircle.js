import styled from 'styled-components';
import { ButtonCircle } from 'rebass';

const StyledButtonCircle = styled(ButtonCircle)`
`;

StyledButtonCircle.defaultProps = {
  color: 'gray.7'
}

export default StyledButtonCircle;