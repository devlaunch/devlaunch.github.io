import styled from 'styled-components';
import { Carousel } from 'rebass';

const StyledCarousel = styled(Carousel)`
`;

StyledCarousel.defaultProps = {
  color: 'gray.7'
}

export default StyledCarousel;