import styled from 'styled-components';
import { BlockLink } from 'rebass';

const StyledBlockLink = styled(BlockLink)`
`;

StyledBlockLink.defaultProps = {
  color: 'gray.7'
}

export default StyledBlockLink;