import styled from 'styled-components';
import { Truncate } from 'rebass';

const StyledTruncate = styled(Truncate)`
`;

StyledTruncate.defaultProps = {
  color: 'gray.7'
}

export default StyledTruncate;